const typeDef = `
  type Photo {
    id: String!
  }
`

export default {
  typeDef
}

// export const query = () => ({
//   getAll: (parent, args, context, info) => {
//     return 'Teams Query'
//   }
// })
